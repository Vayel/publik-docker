{
  "variables": {
    "organization": "${SLUG}",
    "hobo": "${HOBO_SUBDOMAIN}${ENV}.${DOMAIN}",
    "authentic": "${AUTHENTIC_SUBDOMAIN}${ENV}.${DOMAIN}",
    "combo": "${COMBO_SUBDOMAIN}${ENV}.${DOMAIN}",
    "combo_agent": "${COMBO_ADMIN_SUBDOMAIN}${ENV}.${DOMAIN}",
    "passerelle": "${PASSERELLE_SUBDOMAIN}${ENV}.${DOMAIN}",
    "wcs": "${WCS_SUBDOMAIN}${ENV}.${DOMAIN}",
    "fargo": "${FARGO_SUBDOMAIN}${ENV}.${DOMAIN}"
  },
  "steps": [
    {
      "create-hobo": {
        "url": "https://${hobo}/"
      }
    },
    {
      "create-hobo": {
        "url": "https://${organization}.${hobo}/",
        "slug": "hobo-${organization}",
        "title": "${TITLE}",
        "variables": {
          "collectivite": {
            "value": "${TITLE}"
          },
          "collectivite_site": {
            "value": "${SITE_URL}"
          },
          "collectivite_telephone": {
            "value": "${PHONE}"
          },
          "collectivite_email": {
            "value": "${EMAIL}"
          },
          "collectivite_adresse": {
            "value": "${ADDR}"
          },
          "collectivite_adresse_complement": {
            "value": "${ADDR2}"
          },
          "collectivite_code_postal": {
            "value": "${POSTCODE}"
          }
        }
      }
    },
    {
      "set-idp": {
      }
    },
    {
      "create-combo": {
        "url": "https://${organization}.${combo}/",
        "slug": "portal-user-${organization}",
        "title": "Compte citoyen",
        "template_name": "portal-user",
        "variables": {
          "collectivite": {
            "value": "${TITLE}"
          },
          "collectivite_site": {
            "value": "${SITE_URL}"
          },
          "collectivite_telephone": {
            "value": "${PHONE}"
          },
          "collectivite_email": {
            "value": "${EMAIL}"
          },
          "collectivite_adresse": {
            "value": "${ADDR}"
          },
          "collectivite_adresse_complement": {
            "value": "${ADDR2}"
          },
          "collectivite_code_postal": {
            "value": "${POSTCODE}"
          }
        }
      }
    },
    {
      "create-combo": {
        "url": "https://${organization}.${combo_agent}/",
        "slug": "portal-agent-${organization}",
        "title": "Portail agent",
        "template_name": "portal-agent",
        "variables": {
          "collectivite": {
            "value": "${TITLE}"
          },
          "collectivite_site": {
            "value": "${SITE_URL}"
          },
          "collectivite_telephone": {
            "value": "${PHONE}"
          },
          "collectivite_email": {
            "value": "${EMAIL}"
          },
          "collectivite_adresse": {
            "value": "${ADDR}"
          },
          "collectivite_adresse_complement": {
            "value": "${ADDR2}"
          },
          "collectivite_code_postal": {
            "value": "${POSTCODE}"
          }
        }
      }
    },
    {
      "create-passerelle": {
        "url": "https://${organization}.${passerelle}/",
        "slug": "passerelle-${organization}",
        "title": "Passerelle",
        "variables": {
          "collectivite": {
            "value": "${TITLE}"
          },
          "collectivite_site": {
            "value": "${SITE_URL}"
          },
          "collectivite_telephone": {
            "value": "${PHONE}"
          },
          "collectivite_email": {
            "value": "${EMAIL}"
          },
          "collectivite_adresse": {
            "value": "${ADDR}"
          },
          "collectivite_adresse_complement": {
            "value": "${ADDR2}"
          },
          "collectivite_code_postal": {
            "value": "${POSTCODE}"
          }
        }
      }
    },
    {
      "create-wcs": {
        "url": "https://${organization}.${wcs}/",
        "slug": "wcs-${organization}",
        "title": "Démarches",
        "template_name": "publik.zip",
        "variables": {
          "collectivite": {
            "value": "${TITLE}"
          },
          "collectivite_site": {
            "value": "${SITE_URL}"
          },
          "collectivite_telephone": {
            "value": "${PHONE}"
          },
          "collectivite_email": {
            "value": "${EMAIL}"
          },
          "collectivite_adresse": {
            "value": "${ADDR}"
          },
          "collectivite_adresse_complement": {
            "value": "${ADDR2}"
          },
          "collectivite_code_postal": {
            "value": "${POSTCODE}"
          }
        }
      }
    },
    {
      "create-fargo": {
        "url": "https://${organization}.${fargo}/",
        "slug": "fargo-${organization}",
        "title": "Porte-documents",
        "variables": {
          "collectivite": {
            "value": "${TITLE}"
          },
          "collectivite_site": {
            "value": "${SITE_URL}"
          },
          "collectivite_telephone": {
            "value": "${PHONE}"
          },
          "collectivite_email": {
            "value": "${EMAIL}"
          },
          "collectivite_adresse": {
            "value": "${ADDR}"
          },
          "collectivite_adresse_complement": {
            "value": "${ADDR2}"
          },
          "collectivite_code_postal": {
            "value": "${POSTCODE}"
          }
        }
      }
    },
    {
      "set-theme": {
        "theme": "${THEME}" 
      }
    }
  ]
}
